import { LimitarPalavrasPipe } from './limitar-palavras.pipe';

describe('LimitarPalavrasPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitarPalavrasPipe();
    expect(pipe).toBeTruthy();
  });
});
